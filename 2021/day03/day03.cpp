#include "../aoc2021.h"

void day03a(ifstream &file) {
    string line;

    if (!file.is_open())
    {
        perror("File not found");
        return;
    }

    while(file.good())
    {
        getline(file, line);
        int curr = stoi(line);

    }
}

void day03b(ifstream &file) {
    string line;

    if (!file.is_open())
    {
        perror("File not found");
        return;
    }

    while(file.good())
    {
        getline(file, line);
        int curr = stoi(line);

    }
}